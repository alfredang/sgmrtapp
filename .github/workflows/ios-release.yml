name: iOS release

# Auto build + sign + upload + submit to App Store Connect on every push to main.
# Requires these repo secrets:
#   ASC_KEY_ID, ASC_ISSUER_ID, ASC_API_PRIVATE_KEY (the .p8 contents)
#   DIST_CERT_P12_BASE64, DIST_CERT_PASSWORD, PROVISIONING_PROFILE_BASE64
# The SGMRT.xcodeproj is committed (generated from project.yml via XcodeGen); CI uses it directly.

on:
  push:
    branches: [main]
    paths-ignore:
      - "**.md"
      - ".github/**"
      - ".claude/**"
      - "scripts/**"
      - "ci/**"
  workflow_dispatch:

concurrency:
  group: ios-release
  cancel-in-progress: true

jobs:
  release:
    runs-on: macos-15
    env:
      ASC_KEY_ID: ${{ secrets.ASC_KEY_ID }}
      ASC_ISSUER_ID: ${{ secrets.ASC_ISSUER_ID }}
      ASC_API_PRIVATE_KEY: ${{ secrets.ASC_API_PRIVATE_KEY }}
      ASC_BUNDLE_ID: com.alfredang.sgmrt
      TEAM_ID: GU9WTSTX9M
      SCHEME: SGMRT
      PROJECT: SGMRT.xcodeproj
      PROFILE_NAME: SGMRT App Store API
      # Full exact cert name — the "SGMRT App Store API" profile embeds this specific identity.
      # (A bare "iPhone Distribution" gets normalized by Xcode 26 to the Apple Distribution cert,
      # which is NOT in the profile, so archive fails with a cert/profile mismatch.)
      SIGN_IDENTITY: "iPhone Distribution: Alfred Ang (GU9WTSTX9M)"
    steps:
      - uses: actions/checkout@v4

      - name: Select Xcode 26 (App Store requires the iOS 26 SDK)
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Python deps
        run: pip install --quiet cryptography

      - name: Import signing certificate & provisioning profile
        env:
          DIST_CERT_P12_BASE64: ${{ secrets.DIST_CERT_P12_BASE64 }}
          DIST_CERT_PASSWORD: ${{ secrets.DIST_CERT_PASSWORD }}
          PROVISIONING_PROFILE_BASE64: ${{ secrets.PROVISIONING_PROFILE_BASE64 }}
        run: |
          CERT="$RUNNER_TEMP/dist.p12"; PROFILE="$RUNNER_TEMP/profile.mobileprovision"
          KEYCHAIN="$RUNNER_TEMP/app.keychain-db"
          echo "$DIST_CERT_P12_BASE64" | base64 --decode > "$CERT"
          echo "$PROVISIONING_PROFILE_BASE64" | base64 --decode > "$PROFILE"
          security create-keychain -p "" "$KEYCHAIN"
          security set-keychain-settings -lut 21600 "$KEYCHAIN"
          security unlock-keychain -p "" "$KEYCHAIN"
          security import "$CERT" -P "$DIST_CERT_PASSWORD" -A -t cert -f pkcs12 -k "$KEYCHAIN"
          security list-keychain -d user -s "$KEYCHAIN" login.keychain-db
          security set-key-partition-list -S apple-tool:,apple: -k "" "$KEYCHAIN"
          mkdir -p "$HOME/Library/MobileDevice/Provisioning Profiles"
          cp "$PROFILE" "$HOME/Library/MobileDevice/Provisioning Profiles/ci.mobileprovision"

      - name: Determine version & build number
        id: ver
        run: |
          VER=$(python3 scripts/ci_submit.py next-version)
          BUILD=$(date -u +%y%m%d%H%M)
          echo "version=$VER" >> "$GITHUB_OUTPUT"
          echo "build=$BUILD" >> "$GITHUB_OUTPUT"

      - name: Archive
        run: |
          xcodebuild -project "$PROJECT" -scheme "$SCHEME" -configuration Release \
            -archivePath "$RUNNER_TEMP/App.xcarchive" \
            CODE_SIGN_STYLE=Manual "CODE_SIGN_IDENTITY=$SIGN_IDENTITY" \
            "PROVISIONING_PROFILE_SPECIFIER=$PROFILE_NAME" DEVELOPMENT_TEAM=$TEAM_ID \
            MARKETING_VERSION=${{ steps.ver.outputs.version }} \
            CURRENT_PROJECT_VERSION=${{ steps.ver.outputs.build }} \
            clean archive

      - name: Export IPA
        run: |
          xcodebuild -exportArchive -archivePath "$RUNNER_TEMP/App.xcarchive" \
            -exportPath "$RUNNER_TEMP/export" -exportOptionsPlist ExportOptions.plist

      - name: Upload to App Store Connect
        run: |
          mkdir -p "$HOME/.appstoreconnect/private_keys"
          echo "$ASC_API_PRIVATE_KEY" > "$HOME/.appstoreconnect/private_keys/AuthKey_${ASC_KEY_ID}.p8"
          IPA=$(ls "$RUNNER_TEMP/export/"*.ipa | head -1)
          # altool exits 0 even on some upload failures, so check its output explicitly.
          set -o pipefail
          OUT=$(xcrun altool --upload-app -f "$IPA" -t ios --apiKey "$ASC_KEY_ID" --apiIssuer "$ASC_ISSUER_ID" 2>&1) || true
          echo "$OUT"
          if ! echo "$OUT" | grep -q "No errors uploading"; then
            echo "::error::altool upload did not succeed"; exit 1
          fi

      - name: Wait for processing
        run: python3 scripts/ci_submit.py wait-build --build ${{ steps.ver.outputs.build }}

      - name: Attach build & submit for review
        run: python3 scripts/ci_submit.py submit --version ${{ steps.ver.outputs.version }} --build ${{ steps.ver.outputs.build }}
